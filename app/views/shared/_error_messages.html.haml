-if object.errors.any? 
  #error_explanation
    %div.alert-box.alert.radius{"data-alert" => ""}
      %p 
        %b The form contains #{pluralize(object.errors.count, "error")}.   
      %ul
        - object.errors.full_messages.each do |msg|
          %li.error_message
            * #{msg}
      %a.close{href: "#"} ×