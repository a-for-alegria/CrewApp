- provide(:title, "#{@crew.combine_names} profile")

// Modal window
= render 'crew_edit_form'

//Sidebar
= render 'layouts/navbar'

.signed-in-content
  .profile-crew.scrollable
    .details-header-crew
      %h1 #{@crew.combine_names}
    .profile-info-crew
      %h2 Personal info
      %ul#crew_info
        = render 'crew_show'
    %button.btn.btn-default.btn-sm{"data-target" => "#crewEditModal", "data-toggle" => "modal", :type => "button"}
      Edit 
    

          


    
